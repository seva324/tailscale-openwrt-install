# 给你的路由器装一个"内部电话号码" —— 手把手版

这份文档写给**不懂编程的人**。你不需要看懂那些命令是什么意思，照着敲、按菜单选就行。
每一步我都会告诉你三件事：**这是在干什么**、**为什么必须做**、**做完你会看到什么**。

---

## 先说说我们到底想干什么

想象一下：你有一个家，还有一间在外地的书房。家里的电脑、书房的路由器，就像两个**不同的房子**，平时互相看不见。

**Tailscale** 就像一个免费的"内部电话总机"——只要在两边的设备上都登记一下，它们就各得到一个只有你们自己知道的号码（长这样：`100.102.43.42`）。有了这个号码，不管你人在哪，都能直接"打电话"过去。

**装好之后你能做到：**

- 人在家里，想看外地书房那台路由器管着的东西，就像它们在同一个房间一样
- 反过来也一样
- 如果愿意，还能让某个地方的所有上网流量都从另一个地方出去（下面会讲）

---

## 开工之前，先认识三个词

| 词 | 说人话就是 |
|---|---|
| **ShellCrash** | 你路由器里已经装好的一个"上网加速 / 翻墙"程序，它有菜单界面 |
| **内核** | 这个程序里**真正干活的那台发动机**。发动机可以换，我们这次就是要换一台更合适的 |
| **SSH** | 一个"远程敲命令"的通道。就是你在电脑上敲一行字，路由器照着执行 |

还有一个词叫 **压缩包**，就是把一个很大的程序"打包压紧"过。装的时候要先解开才能用。

---

## 开工前请确认

你的路由器**已经装好 ShellCrash**（能进它的菜单）。如果没装，先装那个，再回来。

你还需要**能用 SSH 登进路由器**（密码登录）。这一步通常是你之前设置好的。

⚠️ **一个提醒**：如果你的路由器上**另外还装过一个单独的 Tailscale**，请先把它停掉。
两个 Tailscale 同时跑，就像给同一个房子登记了两个门牌号，会打架。

---

## 第一步：换一台更合适的发动机

### 这是在干什么

把路由器里那台"发动机"换成一台**带 Tailscale 功能**的。

### 为什么必须换

你路由器现在那台发动机，当初为了省地方，**把 Tailscale 功能砍掉了**。
不换的话，后面不管怎么设置都会报错说"这台发动机不支持"。

### 怎么做

在路由器的菜单里，一层一层点进去：

```
主菜单 → 9（更新/升级）
      → 2) 内核
      → 6) 自定义内核
      → 9) 自定义链接
      → 这时会让你粘贴一个网址，把下面这行贴进去：

https://raw.githubusercontent.com/seva324/tailscale-openwrt-install/main/core/sing-box-min-arm64.gz

      → 然后会问你"要装哪种内核"，选  3) Singbox
```

选内核类型那一步长这样，**认准第 3 项**：

```
1) Mihomo(Meta)
2) Singbox-reF1nd      ← 不要选这个
3) Singbox             ← 选这个
4) Clash
```

### 你会看到

它会自己下载、解开、装好，最后提示安装成功。

### 两个容易搞错的地方

- **网址必须是以 `.gz` 结尾的**。别自己改后缀 —— 它靠后缀来判断怎么解压。
- **内核类型必须选 `3) Singbox`**。选错的话，程序会拿错的参数去启动，直接就崩了。

### 如果空间不够

这一步要占大约 **19MB** 的存储空间。如果路由器提示空间不足，
先看看有没有用不上的旧文件可以删掉，或者告诉懂的人来帮忙清理。

---

## 第二步：改一个设置（**这步不能跳**）

### 这是在干什么

把 ShellCrash 的一种工作模式从 `mix` 改成 `fake-ip`。

### 为什么必须做

这就像**汽车的档位挂错了**。`mix` 这个档位，ShellCrash 会去准备一堆路由器**够不着的东西**，
新的发动机看到就会直接拒绝启动 —— 你会看到服务"启动成功"但**实际上网全断**。

这个坑我们踩过两次，所以单独拿出来讲。

### 怎么做

复制下面这行，粘贴到 SSH 里按回车：

```sh
sed -i 's/^dns_mod=.*/dns_mod=fake-ip/' /data/other_vol/ShellCrash/configs/ShellCrash.cfg
```

### 你会看到

**没有任何提示**，就是正常的。这行命令是"默默改掉一个设置"，成功不吭声。

> **注意路径**：上面 `/data/other_vol/ShellCrash` 是常见位置。如果你的是
> `/data/ShellCrash` 或 `/data/other/ShellCrash`，把路径换掉即可。
> 不确定的话先敲 `ls /data` 看看哪个名字对得上。

---

## 第三步：填 Tailscale 的"入场券"

### 这是在干什么

告诉路由器：用哪张"入场券"去登记、登记成什么名字、要开放哪些东西。

### 怎么做

菜单里点：

```
主菜单 → 7) 访问与控制
      → 6) 配置 Tailscale 内网穿透
```

然后像填表格一样，一项一项填：

| 菜单里写的 | 你要填的 | 说明 |
|---|---|---|
| 设置密钥（Auth Key） | `tskey-auth-` 开头的一长串 | 见下方"怎么拿到入场券" |
| 设置设备名称 | 自己起个名字 | **每台机器必须不一样**，比如 `mi-wenzhou` |
| 通告路由内网地址（Subnet） | **打开** | 让外面的人能看到路由器后面的设备 |
| 通告路由全部流量（EXIT-NODE） | 看需要 | 打开 = 让它当"总出口"，见下方说明 |

### 你会看到

填完就生效，不需要额外确认。

### 怎么拿到"入场券"

打开这个网址：https://login.tailscale.com/admin/settings/keys

点生成新密钥，**注意几个选项**：

- **Reusable**（可重复使用）→ **要打开**。这样一张券能给好几台机器用
- **Ephemeral**（临时的）→ **一定不要开**。开了的话机器一重启就"失忆"，白忙活
- **Expiry**（有效期）→ 设成 **Never**（永不）。默认 90 天到期，到时候所有机器会一起掉线
- **Pre-approved** → 打开，省得后面还要一台台批准

### 关于"出口节点"（EXIT-NODE）的两个提醒

**它是什么**：打开它，你的手机或电脑可以选择"所有上网流量都从这台路由器走"。
比如书房那台当出口，你在家上网，网站会以为你在书房。

**两个注意**：

1. **两个开关不要同时打开** —— 程序不允许既"给别人用"又"用别人的"，会直接报错
2. **设备名千万不要两台重名** —— 重名的话第二台会被自动改名成 `名字-1`，
   以后在后台看到两个很像的名字，很容易选错

---

## 第四步：补一条小规则（推荐，但不是必须）

### 这是在干什么

告诉路由器：**"要访问自己家里的设备，直接走过去就行，别绕远路。"**

### 为什么要做

不补这条规则的话，你想通过 Tailscale 访问书房里的打印机、摄像头这些设备时，
流量会被**错误地绕到翻墙线路上**，结果就是 —— 一直转圈，打不开。

### 怎么做

复制这一整段，粘贴到 SSH 里回车：

```sh
GW=/data/other_vol/ShellCrash/jsons/route.json
mkdir -p "$(dirname "$GW")"
cat > "$GW" <<'EOF'
{
  "route": {
    "rules": [
      { "inbound": ["ts-ep"], "ip_is_private": true, "outbound": "DIRECT" }
    ]
  }
}
EOF
```

### 你会看到

同样**没有任何提示**，正常。

---

## 第五步：重启，看看活没活

### 怎么做

三条命令，依次敲：

```sh
/etc/init.d/shellcrash restart
```

```sh
pidof CrashCore
```

```sh
curl -s -o /dev/null -w 'HTTP=%{http_code}\n' --max-time 15 -x http://127.0.0.1:7890 https://www.google.com/generate_204
```

### 你会看到

| 第几条 | 正常的样子 | 说明 |
|---|---|---|
| 第 1 条 | 提示重启成功 | 重启服务 |
| 第 2 条 | **一串数字**，比如 `32651` | 这是发动机的"身份证号"，有数字就说明**活着** |
| 第 3 条 | **`HTTP=204`** | 说明上网功能是通的 |

**如果第 3 条不是 204**：别急，刚重启完可能要等十几秒再试一次。
再试还是不行，看下面"出问题了怎么办"。

---

## 第六步：去后台点一下"批准"

### 这是在干什么

给新登记的这台机器**发放通行证**。

### 为什么要做

Tailscale 出于安全，**新机器说的话不会自动生效**，要你亲自点头。
不点的话，你在别的地方**看不到**这台机器后面有哪些设备。

### 怎么做

1. 打开 https://login.tailscale.com/admin/machines
2. 找到你刚起的那个名字（比如 `mi-wenzhou`）
3. 点 **批准**（Approve）那两个待批准的项：**Subnet routes** 和 **Exit node**

### 你会看到

批准完，那几行"待批准"的提示就消失了。

---

## 什么时候需要"内核态"（可选）

默认情况下，这台路由器**能被外面访问**，但它**自己不能主动访问外面**。
就像门房能给你开门，但门房自己不出门。

如果你希望**路由器后面的设备也能主动去访问别的地方**，那需要开启"内核态"。
这个改动稍微复杂，属于进阶操作 —— 需要的话请找懂的人来弄。

---

## 出问题了怎么办

别按报错原文去搜，**按你看到的现象**对号入座：

| 你看到的现象 | 什么原因 | 怎么办 |
|---|---|---|
| 提示 `Tailscale is not included in this build` | 发动机换错了，装的还是没有 Tailscale 功能的版本 | 回到第一步重装，认准选 `3) Singbox` |
| 提示 `rule_set` 或 `http_client` 相关的错 | 第二步的 `fake-ip` 没改成功 | 回去重做第二步 |
| 服务说"已启动"，但**全屋都上不了网了** | 发动机其实已经死了，但"路障"还立着 | 先敲 `/etc/init.d/shellcrash stop` 恢复上网，再排查 |
| 在别的地方**看不到这台机器** | 后台还没批准 | 回第六步点批准 |
| 能看到机器，但**打不开它后面的设备** | 第四步的规则没加，或者后台没批准 | 都检查一遍 |

### 三条万能排查命令

```sh
pidof CrashCore
```

```sh
df -h /data
```

```sh
tail -30 /tmp/ShellCrash/ShellCrash.log
```

分别看：**发动机活着吗** / **空间够吗** / **最近的记录说了什么**。

---

## 名词小词典

| 词 | 说人话 |
|---|---|
| **内核 / 发动机** | 真正干活的程序。可以换 |
| **压缩包 / .gz** | 打包压紧过的程序，装之前要解开 |
| **Auth Key / 入场券** | 一长串 `tskey-auth-` 开头的密码，用来登记设备 |
| **子网 / Subnet** | 路由器背后的那一串设备（打印机、摄像头、别的电脑） |
| **出口节点 / Exit Node** | 让所有上网流量都从这台机器出去的开关 |
| **Tailnet** | 你自己那个"内部电话网"，只有你的设备在里头 |
| **内核态 / system_interface** | 进阶模式，让路由器自己也能主动访问外面 |
| **重启服务** | 把程序关掉再打开，让新设置生效 |

---

## 一句话回顾

1. **换发动机** —— 菜单 `9 → 2 → 6 → 9`，贴网址，选 `3) Singbox`
2. **改档位** —— `dns_mod` 改成 `fake-ip`（不改必挂）
3. **填入场券** —— 菜单 `7 → 6`，名字每台不同
4. **加小规则** —— 让访问家里设备不绕远路
5. **重启验证** —— 看到 `HTTP=204` 就成功
6. **后台批准** —— 不批准等于没生效
